#include <iostream>
#include <cmath>                    //www.cplusplus.com/reference/cmath/
using namespace std;

int main(){

    double x = 3.14;
    double y = 4;
    double z;

   //  z = max(x, y);        gets the max number 
   //  z = min (x, y);       gets the min number
   //  z = pow(2, 4);        gets 2 to the power of 4
   //  z = sqrt (9);         gets the square root
   //  z = abs(-3);          gets the absolute value
   //  z = round(x);         rounds the value 
   //  z = ceil(x);          rounds the value up
   //  z = floor(x);         rounds the value down


    cout << z;


    return 0;
}